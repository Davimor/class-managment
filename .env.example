# Variables de Entorno - MSSQL Connection
# =========================================

# Credenciales del Servidor MSSQL
MSSQL_SERVER=localhost
MSSQL_PORT=1433
MSSQL_USER=sa
MSSQL_PASSWORD=secret
MSSQL_DATABASE=catequesis
MSSQL_AUTH_TYPE=sql

# Configuración de la Aplicación
JWT_SECRET=tu_clave_secreta_jwt_muy_segura_min_32_caracteres
JWT_EXPIRATION=7d
NODE_ENV=development

# URLs
NEXT_PUBLIC_API_URL=http://localhost:3000
NEXTAUTH_SECRET=tu_secreto_nextauth_muy_seguro_min_32_caracteres

# Verificar también que:
# - MSSQL_SERVER: Puede ser "localhost", una IP, o un FQDN (ej: server.database.windows.net para Azure)
# - MSSQL_PORT: Por defecto 1433 para SQL Server
# - MSSQL_AUTH_TYPE: "sql" para SQL Server Authentication o "windows" para Windows Authentication
# - Si usas Windows Auth, MSSQL_USER debe tener formato: "DOMINIO\usuario"
